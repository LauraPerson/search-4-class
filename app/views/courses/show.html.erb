<h1>All information</h1>
  <%= @course.title %>
  <%= @course.description %>
  <%= @course.price %>
  <%= @course.duration %>
  <%= @course.category %>
  <%= @course.schedule_date %>

<%= link_to "Book this class", course_bookings_path(@course), method: :post %>