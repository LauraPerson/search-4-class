
<h1>Courses list</h1>
<% @courses.each do |course| %>
  <%= course.title %>
  <%= course.description %>
  <%= course.price %>
  <%= course.duration %>
<% end %>

<%= link_to 'New Course', new_course_path %>
